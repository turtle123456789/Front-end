import styled from "styled-components";
import '../../index.css'
export const LayoutRegister = styled.div`
    margin: 0 150px;
    font-size: 16px;
    button{
        background: var(--pink);
        padding: 10px 30px; 
        border-radius: 10px; 
        cursor: pointer; 
        margin: 20px;
    }
`